# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: Name
        span: left
    group:
        label: Group
        type: relation
        span: left

tabs:
    fields:
        properties:
            type: partial
            tab: Properties
            context: update
